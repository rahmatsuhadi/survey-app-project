'use strict';

/**
 * number-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::number-question-answer.number-question-answer');
