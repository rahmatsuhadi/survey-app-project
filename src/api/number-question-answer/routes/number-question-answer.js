'use strict';

/**
 * number-question-answer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::number-question-answer.number-question-answer');
