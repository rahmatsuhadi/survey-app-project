'use strict';

/**
 * text-question-answer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::text-question-answer.text-question-answer');
