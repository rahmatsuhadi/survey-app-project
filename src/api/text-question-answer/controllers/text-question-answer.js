'use strict';

/**
 * text-question-answer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::text-question-answer.text-question-answer');
