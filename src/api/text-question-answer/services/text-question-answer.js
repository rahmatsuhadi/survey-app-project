'use strict';

/**
 * text-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::text-question-answer.text-question-answer');
