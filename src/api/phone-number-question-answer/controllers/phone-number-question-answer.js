'use strict';

/**
 * phone-number-question-answer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::phone-number-question-answer.phone-number-question-answer');
