'use strict';

/**
 * phone-number-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::phone-number-question-answer.phone-number-question-answer');
