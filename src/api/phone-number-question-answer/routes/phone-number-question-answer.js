'use strict';

/**
 * phone-number-question-answer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::phone-number-question-answer.phone-number-question-answer');
