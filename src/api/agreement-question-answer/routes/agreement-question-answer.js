'use strict';

/**
 * agreement-question-answer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::agreement-question-answer.agreement-question-answer');
