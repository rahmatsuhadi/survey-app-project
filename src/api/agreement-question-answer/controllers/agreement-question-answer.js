'use strict';

/**
 * agreement-question-answer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::agreement-question-answer.agreement-question-answer');
