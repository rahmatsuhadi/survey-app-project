'use strict';

/**
 * agreement-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::agreement-question-answer.agreement-question-answer');
