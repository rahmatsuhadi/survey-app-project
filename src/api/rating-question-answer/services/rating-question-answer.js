'use strict';

/**
 * rating-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::rating-question-answer.rating-question-answer');
