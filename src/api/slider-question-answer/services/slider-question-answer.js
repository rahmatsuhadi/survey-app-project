'use strict';

/**
 * slider-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::slider-question-answer.slider-question-answer');
