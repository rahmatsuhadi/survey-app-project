'use strict';

/**
 * email-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::email-question-answer.email-question-answer');
