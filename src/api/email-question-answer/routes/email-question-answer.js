'use strict';

/**
 * email-question-answer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::email-question-answer.email-question-answer');
