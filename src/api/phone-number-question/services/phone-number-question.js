'use strict';

/**
 * phone-number-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::phone-number-question.phone-number-question');
