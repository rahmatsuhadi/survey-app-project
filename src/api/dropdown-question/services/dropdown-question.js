'use strict';

/**
 * dropdown-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dropdown-question.dropdown-question');
