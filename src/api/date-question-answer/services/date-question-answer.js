'use strict';

/**
 * date-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::date-question-answer.date-question-answer');
