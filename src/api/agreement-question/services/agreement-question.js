'use strict';

/**
 * agreement-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::agreement-question.agreement-question');
