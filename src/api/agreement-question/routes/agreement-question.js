'use strict';

/**
 * agreement-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::agreement-question.agreement-question');
