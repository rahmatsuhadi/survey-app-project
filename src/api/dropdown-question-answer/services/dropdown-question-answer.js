'use strict';

/**
 * dropdown-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dropdown-question-answer.dropdown-question-answer');
