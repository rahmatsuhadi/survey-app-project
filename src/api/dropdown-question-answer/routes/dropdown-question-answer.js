'use strict';

/**
 * dropdown-question-answer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::dropdown-question-answer.dropdown-question-answer');
