'use strict';

/**
 * message-question-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::message-question-answer.message-question-answer');
